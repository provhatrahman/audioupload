version: '3.8'
services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./music:/app/music
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1
    command: python app.py